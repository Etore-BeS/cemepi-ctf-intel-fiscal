# Dívida ativa × fatores macroeconômicos

## Pergunta de pesquisa (placeholder)

O estoque/arrecadação no dump cointegra com PIB/Selic/IPCA/desemprego? Eventos (REFIS) mudam o nível?

## Escopo

Painel mensal a partir do dump (e/ou artefatos de monitoramento) + séries externas BCB/IBGE. Dump **não** tem dívida não inscrita.

## Plano / premissa

- Plano C: `docs/planos/C_divida_macro.md` (cópia em `docs/`).
- Premissa dump: `docs/planos/00_premissa_dump.md` / `docs/api-dump/`.

## Shared env

Mono root `.env` + kernel `ctf-research`. Prefer `shared/cemepi_api` and `config.paths` after `uv sync`.
